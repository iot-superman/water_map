# Drinking Water LBS V38

GitHub Pages 單頁版。

## GitHub Pages

1. 將 `index.html` 與 `README.md` 放在 repository 的 `main` branch 根目錄。
2. GitHub → Settings → Pages。
3. Source 選 `Deploy from a branch`。
4. Branch 選 `main`，Folder 選 `/ (root)`。
5. 儲存後等待 GitHub Pages 部署。

網站入口為根目錄的 `index.html`。
